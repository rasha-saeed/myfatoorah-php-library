build:
    nodes:
        analysis:
            dependencies:
                # 🔥 ده السطر المهم جدًا
                override: true

            project_setup:
                override: true

            tests:
                override: true

filter:
    excluded_paths:
        - 'tests/*'
        - 'vendor/*'

checks:
    php: true

tools:
    php_analyzer: true
    php_sim: true
    php_pdepend: true
    php_mess_detector: true

