build:
  nodes:
    analysis:
      project_setup:
        override: true   # 🚫 يمنع composer install
      tests:
        override: true   # 🚫 يمنع phpunit

checks:
  php: true             # ✅ تحليل PHP فقط

tools:
  php_analyzer: true    # ✅ core analyzer
  php_sim: true         # duplication
  php_pdepend: true     # complexity
  php_cpd: true
  php_loc: true

filter:
  excluded_paths:
    - vendor/*
    - tests/*
